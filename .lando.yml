name: empress
recipe: lamp
config:
  webroot: .
services:
  node:
    type: node
    command: npm start
    build:
      - yarn install
    scanner: false
    ssl: false
    sslExpose: false
tooling:
  node:
    service: node
  yarn:
    service: node
  npm:
    service: node